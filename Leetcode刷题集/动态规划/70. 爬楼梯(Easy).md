```js
/**
 * @param {number} n
 * @return {number}
 */
var climbStairs = function(n) {
    let nums = [1, 1]
    for(let i = 2; i <= n; i++) {
        nums[i] = nums[i - 1] + nums[i - 2]
    }

    return nums[n]
};
```